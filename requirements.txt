# Web framework
flask==3.0.3
flask-sqlalchemy==3.1.1
flask-cors==4.0.1

# Database
pymysql==1.1.1
cryptography==43.0.3

# Auth
bcrypt==4.3.0

# ML / data (versions with Python 3.13 pre-built wheels)
scikit-learn==1.6.1
numpy==2.1.3

# NLP (chatbot tokenisation/stop-word removal — falls back to a built-in
# stop-word list if unavailable, but install this for the real NLP path)
nltk==3.9.1

# HTTP (Google Places + Geocoding APIs)
requests==2.32.3

# AI chatbot + packing list generation (Groq/Llama)
groq==1.5.0

# Environment variables
python-dotenv==1.0.1
